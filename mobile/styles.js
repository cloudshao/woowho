import { StyleSheet } from 'react-native';

export default Styles = StyleSheet.create({
  title: {
    marginTop: 90,
    marginBottom: 20,
    fontSize: 40,
    textAlign: 'center',
    color: '#555',
  },
  buttonText: {
    fontSize: 80,
    textAlign: 'center',
    color: 'white',
  },
  name: {
    marginBottom: 10,
    fontSize: 40,
    textAlign: 'center',
    color: '#333',
  },
});
